function Header() {
   return (
      <h1 className="header">
         <span className="icon">💰</span>
         <span className="title">Bank Account</span>
         <span className="icon">💰</span>
      </h1>
   );
}

export default Header;
